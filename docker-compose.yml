services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: florinda
      POSTGRES_PASSWORD: florinda
      POSTGRES_DB: florinda
    ports:
      - "5432:5432"
    volumes:
      - florinda_pg:/var/lib/postgresql/data

volumes:
  florinda_pg:
